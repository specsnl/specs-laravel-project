{{- if UseSonarQube}}
sonar.projectKey={{toKebabCase RepoName | replace "/" "_"}}
sonar.organization=specs

sonar.links.homepage=https://github.com/{{RepoName}}

# This is the name and version displayed in the SonarCloud UI.
sonar.projectName={{ProjectName}}
# sonar.projectVersion=1.0

# Path is relative to the sonar-project.properties file.
sonar.sources=app,config,resources,public,routes

# Encoding of the source code. Default is default system encoding
sonar.sourceEncoding=UTF-8

sonar.php.coverage.reportPaths=reports/phpunit/phpunit-report.xml
sonar.php.phpstan.reportPaths=reports/phpstan/phpstan-report.json

sonar.cpd.exclusions=\
    config/**/*.php

sonar.coverage.exclusions=\
    config/**/*.php
{{- end}}
